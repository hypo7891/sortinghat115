# Magic Weaving

## 啟動

在此資料夾執行：

```powershell
npm.cmd run dev
```

或直接雙擊 `start-game.cmd`，再開啟終端機顯示的本機網址。不要直接開啟原始的 `index.html`。

如果要查看建置結果，請使用 `npm.cmd run preview`，不要直接雙擊 `dist/index.html`。

開始遊戲後，點擊畫布或按 `Space` 發射元素球；移動滑鼠或手指控制魔法擋板。
